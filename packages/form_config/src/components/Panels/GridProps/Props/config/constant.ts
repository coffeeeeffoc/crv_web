// 所有属性的键
export enum AllPropKeys {
  numberOfChildren = 'numberOfChildren',
  numberOfRows = 'numberOfRows',
  numberOfColumns = 'numberOfColumns',
  rowHeight = 'rowHeight',
  rowGap = 'rowGap',
  columnGap = 'columnGap',
  direction = 'direction',
  emptySpace = 'emptySpace',
  alignHorizontal = 'alignHorizontal',
  alignVertical = 'alignVertical',
  itemsHorizontal = 'itemsHorizontal',
  itemsVertical = 'itemsVertical',
};

// grid单位
export enum GridUnit {
  auto = 'auto',
  fr = 'fr',
  minmax = 'minmax',
  px = 'px',
  vh = 'vh',
  vw = 'vw',
  '%' = '%',
};

// tap的单位
export enum GridGapUnit {
  px = 'px',
  vh = 'vh',
  vw = 'vw',
  '%' = '%',
};
